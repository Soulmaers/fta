# PROD Profile Configuration (Main Server)
server.port=8081

# Database PROD (Real Data)
spring.datasource.url=jdbc:postgresql://localhost:5432/fta_prod
spring.datasource.username=postgres
spring.datasource.password=Alexsi02153013

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging
logging.level.com.football.backend=INFO
